This is for programs that access memory in predictable patterns, such as loops that iterate over arrays. Hardware prefetching can be done without software involvement and can be found in most modern CPU's. For example, Intel CPU's feature a variety of prefetch that work across multiple cache levels. Prefetch instructions can be written into the code by the programmer or by the compiler. Prefetch instructions specify the memory addresses to be prefetched and the desired prefetch distance. In software, there are instructions that can be written with: Operating systems use prefetching to reduce file and memory access latency. Web browsers apply prefetching techniques to improve perceived performance. Common examples include: Prefetching can significantly improve performance, but it can not always be beneficial if implemented wrong.